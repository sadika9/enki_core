{
  "name": "@getenki/enki-js",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wasm-pack build --target bundler --out-dir pkg",
    "build:web": "wasm-pack build --target web --out-dir pkg-web",
    "serve:web": "python -m http.server 8080",
    "test:web": "wasm-pack build --target web --out-dir pkg-web"
  }
}
